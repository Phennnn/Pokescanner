# PokeScanner dependencies
#
#   pip install -r requirements.txt
#
# torch/torchvision: the plain wheels are CPU-only, which is all the apps need.
# For GPU training locally, install the CUDA build first, from
# https://pytorch.org/get-started/locally/ , then run this file.

torch>=2.1
torchvision>=0.16
timm>=1.0.0

pillow>=10.0
numpy>=1.24
pandas>=2.0
opencv-python>=4.8

# UIs
gradio>=4.0            # app/app.py
flask>=3.0             # app/pokedex.py
